apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-manager-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock-manager-redis
  template:
    metadata:
      labels:
        app: stock-manager-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: stock-manager-redis
spec:
  selector:
    app: stock-manager-redis
  ports:
    - port: 6379
      targetPort: 6379
