apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-manager-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stock-manager-backend
  template:
    metadata:
      labels:
        app: stock-manager-backend
    spec:
      containers:
        - name: backend
          image: stock-manager-backend:latest
          ports:
            - containerPort: 4000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: stock-manager-secrets
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: stock-manager-secrets
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: stock-manager-backend
spec:
  selector:
    app: stock-manager-backend
  ports:
    - port: 80
      targetPort: 4000
